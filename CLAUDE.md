# Session start

This project runs on Supabase's Free plan (deliberately not upgraded until
sales justify it). At the start of every session, run the Free-tier health
check before doing anything else: see `docs/free-tier-checklist.md` and run
`npm run check-usage`. A 402 must not happen.
